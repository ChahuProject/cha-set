{
  "name": "cha-set",
  "version": "0.1.0",
  "private": true,
  "description": "ChaSet — a cross-stack UI component library (single source of truth drives React and Qt implementations).",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "gen:css": "node spec/generators/generate-css.mjs",
    "gate": "node gate/parity.mjs",
    "typecheck": "pnpm --filter @chaset/ui typecheck",
    "test": "pnpm --filter @chaset/ui test",
    "build": "pnpm --filter @chaset/ui build"
  },
  "engines": {
    "node": ">=20"
  }
}