packages:
  - spec
  - 'packages/*'
  - 'packages/*/examples/*'

onlyBuiltDependencies:
  - esbuild

# pnpm config: allow lefthook build scripts (cross-platform compatibility declaration, requires explicit acknowledgment via pnpm install)
allowBuilds:
  lefthook: true
