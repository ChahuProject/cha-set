# lefthook.yml — git hooks config (lefthook layer)
commit-msg:
  commands:
    commitlint:
      # Use pnpm exec (commitlint installed locally) instead of npx; "{1}" is a lefthook placeholder (cross-platform, do not use $1)
      run: pnpm exec commitlint --edit "{1}"